---
phase: 01-foundation-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/index.html
  - frontend/src/index.css
  - frontend/src/lib/utils.ts
  - frontend/components.json
  - frontend/src/components/ui/button.tsx
  - frontend/src/components/ui/input.tsx
  - frontend/src/components/ui/card.tsx
  - frontend/src/components/ui/form.tsx
  - frontend/src/components/ui/label.tsx
autonomous: false
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-04
user_setup:
  - service: npm registry (npmjs.com)
    why: "Verify package legitimacy before installing new dependencies"
    env_vars: []
    dashboard_config:
      - task: "For each package: open npmjs.com/package/{name}, verify it is the real package (not a typosquat), check download counts and publish dates"
      - task: "Start the backend before testing in Plan 01-02: cd backend && npm install && npm run dev"
must_haves:
  truths:
    - "frontend/ directory exists with the adapted scaffold (not in frontend-to-implement/ anymore)"
    - "frontend/package.json has no @google/genai, express, dotenv, or autoprefixer dependencies"
    - "frontend/vite.config.ts proxies /api requests to localhost:4000"
    - "npm run dev in frontend/ starts a Vite dev server on port 3000 without errors"
    - "shadcn/ui Button, Input, Card, Form, and Label components exist in frontend/src/components/ui/"
    - "frontend/src/index.css no longer has the universal * { border-radius: 0 !important } rule that conflicts with shadcn"
  artifacts:
    - path: "frontend/package.json"
      provides: "Project dependencies manifest"
      contains: "react-hook-form"
    - path: "frontend/vite.config.ts"
      provides: "Vite build config with API proxy and shadcn-compatible @ alias"
      contains: "path.resolve(__dirname, './src')"
    - path: "frontend/src/lib/utils.ts"
      provides: "shadcn/ui cn() CSS class merge utility"
      exports: ["cn"]
    - path: "frontend/components.json"
      provides: "shadcn/ui configuration"
      contains: "style"
    - path: "frontend/src/components/ui/button.tsx"
      provides: "shadcn Button component"
    - path: "frontend/src/components/ui/input.tsx"
      provides: "shadcn Input component"
    - path: "frontend/src/components/ui/card.tsx"
      provides: "shadcn Card component"
    - path: "frontend/src/components/ui/form.tsx"
      provides: "shadcn Form component (react-hook-form integration)"
    - path: "frontend/src/components/ui/label.tsx"
      provides: "shadcn Label component"
  key_links:
    - from: "frontend/vite.config.ts"
      to: "frontend/src/"
      via: "Vite resolve.alias @ -> ./src"
      pattern: "path\\.resolve\\(__dirname, '\\./src'\\)"
    - from: "frontend/src/components/ui/button.tsx"
      to: "frontend/src/lib/utils.ts"
      via: "import { cn }"
      pattern: "from '@/lib/utils'"
---

<objective>
Adapt the AI Studio-generated scaffold (`frontend-to-implement/`) into the production `frontend/` directory. Strip Express server, @google/genai, dotenv, and autoprefixer dependencies per STACK.md. Install new dependencies (axios, react-hook-form, zod, @hookform/resolvers, sonner, react-dropzone). Initialize shadcn/ui and add the Button, Input, Card, Form, and Label components. Configure Vite proxy to forward /api requests to the real backend on port 4000.

Purpose: Establish the correct project directory, dependency stack, and UI component library that Plans 01-02 and all subsequent phases build on.
Output: A working `frontend/` directory that runs with `npm run dev` on port 3000, has shadcn/ui components available, and proxies API calls — but still uses the scaffold's mock `apiFetch` (replaced in Plan 01-02).
</objective>

<execution_context>
@/Users/quesadx/github/RecipeHub/.opencode/gsd-core/workflows/execute-plan.md
@/Users/quesadx/github/RecipeHub/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-authentication/01-RESEARCH.md
@.planning/phases/01-foundation-authentication/01-PATTERNS.md
@.planning/phases/01-foundation-authentication/01-UI-SPEC.md
@.planning/phases/01-foundation-authentication/SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Directory rename, package.json cleanup, vite config, and index.html</name>
  <files>frontend/package.json, frontend/vite.config.ts, frontend/index.html</files>

  <read_first>
  - frontend-to-implement/package.json — current scaffold dependencies (source of truth before editing)
  - frontend-to-implement/vite.config.ts — current scaffold Vite config (lines 1-22)
  - frontend/vite.config.js — existing frontend proxy config to merge (lines 1-14)
  - frontend-to-implement/index.html — current scaffold HTML (line 5 title to change)
  - .planning/phases/01-foundation-authentication/01-RESEARCH.md — Section "Standard Stack" for exact version numbers, Section "Runtime State Inventory" for git migration steps
  - .planning/phases/01-foundation-authentication/01-PATTERNS.md — Sections "frontend/package.json" (line 742-767), "frontend/vite.config.ts" (line 672-739)
  </read_first>

  <action>
  **Step A: Rename the directory (SETUP-01, SETUP-04 per RESEARCH.md Runtime State Inventory).**
  1. Remove old `frontend/` from git tracking: `git rm -r frontend/`
  2. Rename scaffold: `git mv frontend-to-implement frontend`
  3. Both operations must succeed. The old `frontend/` directory is fully removed; the scaffold is now at `frontend/`.

  **Step B: Edit frontend/package.json (SETUP-01).**
  Remove these entries from `dependencies`:
  - `"@google/genai": "^2.4.0"` (Gemini SDK — no AI features in scope)
  - `"express": "^4.21.2"` (frontend does not need its own Express server)
  - `"dotenv": "^17.2.3"` (Vite uses `import.meta.env` natively)
  Remove these entries from `devDependencies`:
  - `"@types/express": "^4.17.21"` (types for removed Express)
  - `"@types/node": "^22.14.0"` (types for removed server-side code)
  - `"autoprefixer": "^10.4.21"` (Tailwind v4 Vite plugin handles this)
  - `"esbuild": "^0.25.0"` (server-side build tool; not needed)
  - `"tsx": "^4.21.0"` (TypeScript executor for server code; not needed)
  Add to `dependencies`:
  - `"axios": "^1.17.0"`
  - `"react-hook-form": "^7.77.0"`
  - `"zod": "^3.25.76"`
  - `"@hookform/resolvers": "^5.4.0"`
  - `"sonner": "^2.0.7"`
  - `"react-dropzone": "^15.0.0"`
  Modify existing entries:
  - Change `"react-router-dom"` from `"^7.16.0"` to `"~6.30.4"` (downgrade to stable SPA router v6)
  - Change `"@tailwindcss/vite"` from `"^4.1.14"` to `"^4.3.0"` (bump per STACK.md)
  Move `tailwindcss` from `devDependencies` to `dependencies`: delete from devDeps, add `"tailwindcss": "^4.3.0"` to deps (bump version, move location — required peer of @tailwindcss/vite at build time)
  Keep all other entries unchanged: `lucide-react`, `motion`, `react`, `react-dom`, `vite`, `typescript`, `@vitejs/plugin-react`.
  Update `"scripts"`: remove `"clean": "rm -rf dist server.js"` (no server.js in frontend). Keep `"dev"`, `"build"`, `"preview"`, `"lint"`.

  **Step C: Edit frontend/vite.config.ts (SETUP-01).**
  Read the current file. Make these changes:
  1. Change the `@` alias from `path.resolve(__dirname, '.')` to `path.resolve(__dirname, './src')` — shadcn/ui v4 init expects `@/` to map to `src/`, not project root
  2. Remove the entire `server: { hmr: ..., watch: ... }` block (lines 14-20 — AI Studio-specific HMR disable logic, not needed in production)
  3. Replace the removed server block with: `server: { port: 3000, proxy: { '/api': { target: 'http://localhost:4000', changeOrigin: true } } }`
  4. The `plugins: [react(), tailwindcss()]` line stays (confirmed working with React 19 + Tailwind v4)
  5. Remove the function wrapper — change `export default defineConfig(() => { return { ... }; });` to `export default defineConfig({ ... });` (simpler, no dynamic config needed)

  **Step D: Edit frontend/index.html.**
  Change line 5: `<title>My Google AI Studio App</title>` → `<title>RecipeHub</title>`
  </action>

  <verify>
    <automated>
      # Directory rename
      test -d frontend/src && echo "PASS: frontend/ exists" || echo "FAIL: frontend/ missing"
      test ! -d frontend-to-implement && echo "PASS: old directory removed" || echo "FAIL: frontend-to-implement/ still exists"

      # Package.json cleanup
      node -e "const p=require('./frontend/package.json'); ['@google/genai','express','dotenv','autoprefixer'].forEach(d=>{if(p.dependencies&&p.dependencies[d])throw new Error(d+' still in dependencies'); if(p.devDependencies&&p.devDependencies[d])throw new Error(d+' still in devDependencies')}); console.log('PASS: stripped deps removed')"
      node -e "const p=require('./frontend/package.json'); ['axios','react-hook-form','zod','@hookform/resolvers','sonner','react-dropzone'].forEach(d=>{if(!p.dependencies[d])throw new Error(d+' missing from dependencies')}); console.log('PASS: new deps present')"
      node -e "const p=require('./frontend/package.json'); const v=p.dependencies['react-router-dom']; if(!v.includes('6.')) throw new Error('react-router-dom not v6: '+v); console.log('PASS: react-router-dom v6')"

      # Vite config
      node -e "const fs=require('fs'); const c=fs.readFileSync('frontend/vite.config.ts','utf8'); if(!c.includes(\"path.resolve(__dirname, './src')\")) throw new Error('@ alias not src/'); if(!c.includes(\"'/api'\")) throw new Error('proxy /api missing'); if(!c.includes('4000')) throw new Error('proxy target 4000 missing'); console.log('PASS: vite.config.ts correct')"

      # Index.html title
      node -e "const fs=require('fs'); const h=fs.readFileSync('frontend/index.html','utf8'); if(!h.includes('<title>RecipeHub</title>')) throw new Error('title not RecipeHub'); console.log('PASS: index.html title')"
    </automated>
  </verify>

  <done>
  - `frontend/` directory is the adapted scaffold (old `frontend/` removed, `frontend-to-implement/` renamed)
  - `frontend/package.json` has no @google/genai, express, dotenv, autoprefixer; has axios, react-hook-form, zod, @hookform/resolvers, sonner, react-dropzone; react-router-dom is at ~6.30.4; tailwindcss and @tailwindcss/vite at ^4.3.0
  - `frontend/vite.config.ts` proxies /api → localhost:4000 and has @ alias pointing to ./src
  - `frontend/index.html` title is "RecipeHub"
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Package legitimacy verification, npm install, shadcn/ui init, and shadcn components</name>
  <files>
    frontend/node_modules/,
    frontend/package-lock.json,
    frontend/src/lib/utils.ts,
    frontend/components.json,
    frontend/src/components/ui/button.tsx,
    frontend/src/components/ui/input.tsx,
    frontend/src/components/ui/card.tsx,
    frontend/src/components/ui/form.tsx,
    frontend/src/components/ui/label.tsx,
    frontend/src/index.css
  </files>

  <read_first>
  - frontend/package.json — to confirm edited state before npm install
  - frontend-to-implement/src/index.css — current scaffold CSS to be edited (lines 27-30 contain the conflicting `* { border-radius: 0px !important }` rule)
  - .planning/phases/01-foundation-authentication/01-RESEARCH.md — Section "Package Legitimacy Audit" (lines 75-92) for [ASSUMED] package list and checkpoint rationale
  - .planning/phases/01-foundation-authentication/01-UI-SPEC.md — lines 28-29 for shadcn init CSS merge instructions
  - .planning/phases/01-foundation-authentication/01-PATTERNS.md — Sections "frontend/src/index.css" (line 622-668) for CSS merge guide, "frontend/src/lib/utils.ts" (line 770-783) for expected cn() pattern, "frontend/components.json" (line 788-793), "frontend/src/components/ui/*" (line 796-806)
  </read_first>

  <action>
  **Step A: Package legitimacy gate (BLOCKING — do not proceed until human approves).**
  RESEARCH.md flagged all 6 new packages as `[ASSUMED]` because slopcheck was unavailable. Before installing, present this list to the human:

  | Package | Version | npm URL | Weekly Downloads |
  |---------|---------|---------|-----------------|
  | axios | 1.17.0 | https://www.npmjs.com/package/axios | ~50M |
  | react-hook-form | 7.77.0 | https://www.npmjs.com/package/react-hook-form | ~5M |
  | zod | 3.25.76 | https://www.npmjs.com/package/zod | ~12M |
  | @hookform/resolvers | 5.4.0 | https://www.npmjs.com/package/@hookform/resolvers | ~3M |
  | sonner | 2.0.7 | https://www.npmjs.com/package/sonner | ~1.5M |
  | react-dropzone | 15.0.0 | https://www.npmjs.com/package/react-dropzone | ~3M |

  The human must verify each package on npmjs.com (not a typosquat, reasonable download counts, legitimate publisher) and type the resume signal.

  **Step B: npm install (SETUP-02).**
  After human approval, run: `cd frontend && npm install`
  This installs all deps (new + existing) based on the edited `package.json`.
  Verify with: `cd frontend && npx tsc --noEmit` — the scaffold source code should still compile. The old `apiFetch`-based pages compile because `apiFetch` is still defined in `src/api/client.ts` (hasn't been replaced yet).

  **Step C: Initialize shadcn/ui (SETUP-02).**
  Run: `cd frontend && npx shadcn@latest init`
  Interactive prompts — answer:
  - "Which style would you like to use?" → Default
  - "Which color would you like to use as the base color?" → Neutral
  - "Where is your global CSS file?" → src/index.css
  - "Do you want to use CSS variables for theming?" → **No** (scaffold already uses Tailwind v4 `@theme` with CSS custom properties — this is equivalent; choosing Yes would add a conflicting shadcn theme layer. Per UI-SPEC line 29: "answer 'no' to CSS variable prompt")
  - "Where is your tailwind.config located?" → (not asked in Tailwind v4 — skip)
  - "Configure import alias for components?" → @/components
  - "Configure import alias for utils?" → @/lib/utils

  After init, verify `frontend/components.json` and `frontend/src/lib/utils.ts` were created.

  **Step D: Add shadcn/ui components (SETUP-02).**
  Run: `cd frontend && npx shadcn@latest add button input card form label`
  This copies 5 components into `frontend/src/components/ui/`. Verify each `.tsx` file exists.

  **Step E: Edit frontend/src/index.css — remove universal CSS reset that conflicts with shadcn.**
  Per UI-SPEC line 29 and PATTERNS.md line 667-668: the `* { border-radius: 0px !important; box-shadow: none !important; }` rule (scaffold lines 27-30) suppresses shadcn's built-in border-radius and shadow tokens, causing shadcn components to render with zero-radius corners and no shadows.

  Delete the entire `* { ... }` ruleset (lines 27-30 of the original scaffold file). Keep everything else: Google Fonts @import, @tailwind import, @theme block (all color + font custom properties), body rule.

  **Step F: Verify the app compiles.**
  Run: `cd frontend && npx tsc --noEmit`
  Must pass (0 errors). The scaffold pages that import `{ apiFetch }` still work because `src/api/client.ts` still exports `apiFetch` (has not been replaced yet — that happens in Plan 01-02).
  </action>

  <how-to-verify>
  1. For each of the 6 packages, visit the npmjs.com URL in a browser
  2. Check: package name matches exactly (no typosquat variations), download counts are in the millions (not hundreds), publisher is the legitimate maintainer
  3. Type "approved" or describe any concerns
  </how-to-verify>

  <resume-signal>Type "approved" to proceed with npm install and shadcn setup, or describe issues</resume-signal>

  <verify>
    <automated>
      # npm install succeeded
      test -d frontend/node_modules && echo "PASS: node_modules exists" || echo "FAIL: node_modules missing"
      test -f frontend/package-lock.json && echo "PASS: package-lock.json exists" || echo "FAIL: package-lock.json missing"

      # shadcn init — components.json exists
      test -f frontend/components.json && echo "PASS: components.json exists" || echo "FAIL: components.json missing"
      # shadcn init — lib/utils.ts exists with cn export
      grep -q 'export function cn' frontend/src/lib/utils.ts 2>/dev/null && echo "PASS: cn() function exported" || echo "FAIL: cn() not found in lib/utils.ts"

      # shadcn components — all 5 added
      for comp in button input card form label; do
        test -f "frontend/src/components/ui/${comp}.tsx" && echo "PASS: $comp.tsx" || echo "FAIL: $comp.tsx missing"
      done

      # index.css — conflicting * rule REMOVED
      grep -c 'border-radius: 0px !important' frontend/src/index.css | xargs -I{} sh -c 'test {} -eq 0 && echo "PASS: border-radius:0px rule removed" || echo "FAIL: border-radius:0px rule still present"'

      # TypeScript compilation
      cd frontend && npx tsc --noEmit && echo "PASS: tsc --noEmit" || echo "FAIL: tsc compilation errors"
    </automated>
  </verify>

  <done>
  - All 6 new packages installed in frontend/node_modules/ (verified by package-lock.json)
  - shadcn/ui initialized (components.json exists, lib/utils.ts has cn())
  - shadcn Button, Input, Card, Form, and Label components exist in src/components/ui/
  - frontend/src/index.css has no `* { border-radius: 0px !important; box-shadow: none !important; }` rule
  - `cd frontend && npx tsc --noEmit` passes (0 errors)
  - `cd frontend && npm run dev` starts Vite dev server on port 3000 (scaffold pages render, still using mock data)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → project | npm packages are downloaded and executed during install and build. Malicious packages can execute postinstall scripts, steal env vars, or inject code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm install of axios, react-hook-form, zod, @hookform/resolvers, sonner, react-dropzone | mitigate | Blocking human checkpoint in Task 2: verify each package at npmjs.com before install. All packages are [ASSUMED] per RESEARCH.md Package Legitimacy Audit. None have postinstall scripts. |
| T-01-01 | Information Disclosure | package.json dependency versions | accept | Dependency versions are public metadata. No secrets in package.json. `.env.example` placeholders are stripped. |
</threat_model>

<verification>
1. `cd frontend && npm run dev` starts on port 3000 without errors
2. Opening `http://localhost:3000` renders the scaffold pages (mock data still works via old apiFetch)
3. `frontend/package.json` has no Express, Gemini, dotenv, autoprefixer dependencies (visual inspection or grep)
4. `curl http://localhost:3000/api/auth/login` returns proxy error (expected — backend not running yet, but Vite proxy is configured)
</verification>

<success_criteria>
- The `frontend/` directory is a working React 19 + TypeScript + Vite 6 project adapted from the AI Studio scaffold
- All Express/Gemini/dotenv/autoprefixer dependencies are stripped from package.json
- All new dependencies (axios, react-hook-form, zod, @hookform/resolvers, sonner, react-dropzone) are installed at correct versions
- shadcn/ui is initialized and Button, Input, Card, Form, Label components are available
- Vite dev server proxies /api/* to the backend on port 4000
- `npm run dev` starts successfully with zero TypeScript errors
</success_criteria>

<output>
Create `.planning/phases/01-foundation-authentication/01-01-SUMMARY.md` when done
</output>
