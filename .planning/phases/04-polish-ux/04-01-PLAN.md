---
phase: 04-polish-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Header.tsx
  - frontend/src/pages/Profile.tsx
  - frontend/src/pages/Register.tsx
  - frontend/src/pages/CreateRecipe.tsx
  - frontend/src/pages/EditRecipe.tsx
  - frontend/src/pages/Home.tsx
  - frontend/src/pages/RecipeDetail.tsx
  - frontend/src/pages/Login.tsx
  - frontend/src/pages/NotFound.tsx
  - frontend/src/pages/Unauthorized.tsx
autonomous: true
requirements: [UX-01]

must_haves:
  truths:
    - '"New Recipe" nav link in Header navigates to /create (not 404 via /new)'
    - 'Profile page loads user recipes from real API (not ReferenceError from removed apiFetch)'
    - '"Create my first recipe" CTA link on Profile goes to /create (not /new)'
    - 'Success toast messages use exact UI-SPEC copy: "Account created successfully!", "Recipe published successfully!", "Recipe updated successfully!"'
    - 'Page root elements use tw-animate-css composition API (animate-in fade-in duration-300) instead of the no-op animate-fade-in'
    - 'EditRecipe error heading uses correct class text-text-custom (not non-existent text-text-cancel)'
  artifacts:
    - path: "frontend/src/components/Header.tsx"
      provides: "Navigation with correct /create route"
      contains: 'to="/create"'
    - path: "frontend/src/pages/Profile.tsx"
      provides: "Working Profile page with axios API, correct routes"
      contains: "import { api }"
    - path: "frontend/src/pages/Register.tsx"
      provides: "Correct success toast message per copywriting contract"
      contains: "Account created successfully!"
    - path: "frontend/src/pages/CreateRecipe.tsx"
      provides: "Correct success toast message"
      contains: "Recipe published successfully!"
    - path: "frontend/src/pages/EditRecipe.tsx"
      provides: "Correct success toast + no CSS typo"
      contains: "Recipe updated successfully!"
  key_links:
    - from: "frontend/src/components/Header.tsx"
      to: "/create route in App.tsx"
      via: 'NavLink to="/create"'
      pattern: 'to="/create"'
    - from: "frontend/src/pages/Profile.tsx"
      to: "frontend/src/api/client.ts"
      via: "import { api } from '../api/client'"
      pattern: "import.*api.*from.*client"
    - from: "frontend/src/pages/Profile.tsx"
      to: "/create route"
      via: 'Link to="/create"'
      pattern: 'to="/create"'
---

## Phase Goal

**As a** RecipeHub user, **I want to** navigate all routes without hitting broken links and see correct toast messages after form submissions, **so that** the app feels reliable and polished.

<objective>
Fix three critical bugs (routing dead links, broken Profile page), correct data integrity issues (toast messages per UI-SPEC copywriting contract, CSS typo, no-op animation class), and verify responsive design compliance across all breakpoints.

Purpose: These bugs block Phase 04 — users hit 404 on "New Recipe" links and the Profile page crashes at runtime. UX-01 (responsive design) cannot be verified until all pages are functional.
Output: 10 files modified; all pages navigable; toast messages match UI-SPEC contract; page transition animation works; responsive design audit confirmed passing (0 layout breakages).
</objective>

<execution_context>
@/Users/quesadx/github/RecipeHub/.opencode/gsd-core/workflows/execute-plan.md
@/Users/quesadx/github/RecipeHub/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-polish-ux/04-RESEARCH.md
@.planning/phases/04-polish-ux/04-UI-SPEC.md

# Source files (read before editing):
@frontend/src/components/Header.tsx
@frontend/src/pages/Profile.tsx
@frontend/src/pages/Register.tsx
@frontend/src/pages/CreateRecipe.tsx
@frontend/src/pages/EditRecipe.tsx
@frontend/src/pages/Home.tsx
@frontend/src/pages/RecipeDetail.tsx
@frontend/src/pages/Login.tsx
@frontend/src/pages/NotFound.tsx
@frontend/src/pages/Unauthorized.tsx
@frontend/src/api/client.ts

# Reference implementation (correct patterns to replicate):
@frontend/src/pages/Home.tsx (correct Skeleton grid pattern, correct error banner)
@frontend/src/api/recipes.ts (correct api.get/delete patterns, toast usage pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 3 Critical Bugs — Routing + Broken Profile Page</name>
  <files>frontend/src/components/Header.tsx, frontend/src/pages/Profile.tsx</files>
  <read_first>
    - frontend/src/components/Header.tsx — current state with /new link on line 42
    - frontend/src/pages/Profile.tsx — current state with @ts-nocheck, apiFetch imports, /new link
    - frontend/src/api/client.ts — confirms only { api } is exported (no apiFetch)
    - frontend/src/api/recipes.ts — reference: api.get returns Promise that unwraps to { data: Recipe[] }, api.delete returns Promise<void>
  </read_first>
  <action>
    Fix three bugs blocking user navigation and page functionality, per RESEARCH.md §CRITICAL BUGS:

    **BUG-1 — Header.tsx line 42:**
    - Change `to="/new"` to `to="/create"` on the `<NavLink>` for "New Recipe"
    - The route `/create` was wired in App.tsx during Phase 03 (03-01); `/new` has never existed

    **BUG-2 — Profile.tsx line 202:**
    - Change `to="/new"` to `to="/create"` on the `<Link>` for "Create my first recipe" in the empty state CTA

    **BUG-3 — Profile.tsx apiFetch→api (entire file):**
    - Remove line 1 `// @ts-nocheck — Phase 1 stub; re-implemented with axios in Phase 2`
    - Line 11: Change `import { apiFetch } from '../api/client'` to `import { api } from '../api/client'`
    - Line 31: Change `const data: Recipe[] = await apiFetch('/api/recipes')` to:
      ```
      const res = await api.get('/api/recipes');
      const data: Recipe[] = res.data;
      ```
      (api.get returns AxiosResponse; must unwrap .data. This is the critical change — apiFetch returned data directly.)
    - Line 62: Change `await apiFetch(\`/api/recipes/${recipeToDelete}\`, { method: 'DELETE' })` to:
      `await api.delete(\`/api/recipes/${recipeToDelete}\`)`
      (api.delete returns Promise<void> per the deleteRecipe wrapper pattern in recipes.ts line 53-54)

    **CRITICAL RULES:**
    - Do NOT change any other Profile.tsx logic (fetch filtering, delete confirmation, skeleton divs, empty state markup)
    - Do NOT add sonner import or convert alert() to toast (out of scope for this bug fix)
    - Preserve all existing import lines except the apiFetch→api change
    - Verify TypeScript compiles with `cd frontend && npx tsc --noEmit` — target 0 new errors from these changes
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -c "Profile.tsx\|Header.tsx" ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - Header.tsx contains `to="/create"` (grep count = 1 for exact string `to="/create"`)
    - Header.tsx does NOT contain `to="/new"` (grep count = 0)
    - Profile.tsx does NOT contain `@ts-nocheck` (grep count = 0)
    - Profile.tsx does NOT contain `apiFetch` (grep count = 0)
    - Profile.tsx imports `{ api }` from `'../api/client'` (grep count = 1 for `import { api }`)
    - Profile.tsx contains `api.get('/api/recipes')` (grep count = 1)
    - Profile.tsx contains `res.data` on the line following `api.get` (grep count = 1 for `.data` in Recipe[] assignment context)
    - Profile.tsx contains `api.delete(` (grep count = 1)
    - Profile.tsx contains `to="/create"` (grep count = 1)
    - Profile.tsx does NOT contain `to="/new"` (grep count = 0)
    - `npx tsc --noEmit` shows no NEW errors from Header.tsx or Profile.tsx (pre-existing RecipeForm.tsx errors are unrelated)
  </acceptance_criteria>
  <done>Users click "New Recipe" in Header → land on CreateRecipe page. Profile page loads recipes from real API without ReferenceError. "Create my first recipe" CTA navigates to /create.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Data Integrity — Toast Messages, CSS Typo, Page Transition Animation</name>
  <files>
    frontend/src/pages/Register.tsx,
    frontend/src/pages/CreateRecipe.tsx,
    frontend/src/pages/EditRecipe.tsx,
    frontend/src/pages/Home.tsx,
    frontend/src/pages/RecipeDetail.tsx,
    frontend/src/pages/Profile.tsx,
    frontend/src/pages/Login.tsx,
    frontend/src/pages/NotFound.tsx,
    frontend/src/pages/Unauthorized.tsx
  </files>
  <read_first>
    - frontend/src/pages/Register.tsx — line 47 toast message, line 56 root div className
    - frontend/src/pages/CreateRecipe.tsx — line 21 toast message, line 33 root div className
    - frontend/src/pages/EditRecipe.tsx — line 47 toast message, line 105 typo + heading, line 120 root div className
    - frontend/src/pages/Home.tsx — line 110 root div className
    - frontend/src/pages/RecipeDetail.tsx — line 215 root div className
    - frontend/src/pages/Profile.tsx — line 93 root div className
    - frontend/src/pages/Login.tsx — line 62 root div className
    - frontend/src/pages/NotFound.tsx — line 7 root div className
    - frontend/src/pages/Unauthorized.tsx — line 11 root div className
    - @.planning/phases/04-polish-ux/04-UI-SPEC.md — §Copywriting Contract (toast messages table, lines 158-165), §Design System (no animate-fade-in utility defined)
    - @.planning/phases/04-polish-ux/04-RESEARCH.md — §Toast Message Audit (lines 194-206), §Responsive Issues Found (lines 249-256)
  </read_first>
  <action>
    Fix three categories of data integrity issues identified in RESEARCH.md §Toast Message Audit, §Error State Audit, and §Responsive Issues:

    **Category A — Toast Messages (3 files, exact string replacements):**
    - Register.tsx line 47: `'Account created!'` → `'Account created successfully!'`
    - CreateRecipe.tsx line 21: `'Recipe published!'` → `'Recipe published successfully!'`
    - EditRecipe.tsx line 47: `'Recipe updated!'` → `'Recipe updated successfully!'`
    (These match UI-SPEC §Copywriting Contract §Standardized Form Submission States, lines 158-165)

    **Category B — EditRecipe.tsx Error State Fixes (2 changes, same file):**
    - Line 105: In the `<p className="...">` for the error heading, change `text-text-cancel` to `text-text-custom`
      (text-text-cancel is not a CSS variable — it silently resolves to no color. text-text-custom = #1a1a1a is the correct primary text color.)
    - Line 105: Change heading text from `"Retrieval Failed"` to `"Unable to Load Recipe"`
      (per UI-SPEC §Standardized Error States, lines 148-155, and RESEARCH.md §Error State Audit, line 182)

    **Category C — animate-fade-in Replacement (9 files, identical single-class replacement):**
    The class `animate-fade-in` is a no-op — `tw-animate-css` provides `fade-in` as a utility composed with `animate-in`, not as a standalone animation class. Currently, 9 page root elements use `animate-fade-in` with zero visual effect.
    - **Fix**: In each of the 9 pages, on the page root `<div>` element (the one with `id="..."` or the main wrapper), replace `animate-fade-in` with `animate-in fade-in duration-300`:
      - Home.tsx line 110: `className="space-y-8 animate-fade-in"` → `className="space-y-8 animate-in fade-in duration-300"`
      - RecipeDetail.tsx line 215: change `animate-fade-in` to `animate-in fade-in duration-300`
      - Profile.tsx line 93: change `animate-fade-in` to `animate-in fade-in duration-300`
      - CreateRecipe.tsx line 33: change `animate-fade-in` to `animate-in fade-in duration-300`
      - EditRecipe.tsx line 120: change `animate-fade-in` to `animate-in fade-in duration-300`
      - Login.tsx line 62: change `animate-fade-in` to `animate-in fade-in duration-300`
      - Register.tsx line 56: change `animate-fade-in` to `animate-in fade-in duration-300`
      - NotFound.tsx line 7: change `animate-fade-in` to `animate-in fade-in duration-300`
      - Unauthorized.tsx line 11: change `animate-fade-in` to `animate-in fade-in duration-300`
    - Do NOT alter any other class names on these elements — only the `animate-fade-in` token within the className string

    **CRITICAL RULES:**
    - Replace ONLY the exact strings specified above; do not refactor surrounding code
    - Do not modify any other toast calls (Login, RecipeDetail comment, RecipeDetail delete — these already match UI-SPEC)
    - Verify all 9 pages have zero remaining instances of `animate-fade-in` after changes
  </action>
  <verify>
    <automated>cd frontend && grep -r "animate-fade-in" src/pages/ src/App.tsx && echo "FAIL: animate-fade-in still present" || echo "PASS: animate-fade-in removed"</automated>
  </verify>
  <acceptance_criteria>
    - Register.tsx contains `'Account created successfully!'` (grep count = 1)
    - CreateRecipe.tsx contains `'Recipe published successfully!'` (grep count = 1)
    - EditRecipe.tsx contains `'Recipe updated successfully!'` (grep count = 1)
    - EditRecipe.tsx does NOT contain `text-text-cancel` (grep count = 0)
    - EditRecipe.tsx contains `"Unable to Load Recipe"` (grep count = 1)
    - Zero occurrences of `animate-fade-in` in frontend/src/ (grep -r count = 0)
    - All 9 page root elements use `animate-in fade-in duration-300` (grep -r count ≥ 9 for exact string)
    - `npx tsc --noEmit` shows no new errors (pre-existing RecipeForm.tsx errors unrelated)
  </acceptance_criteria>
  <done>All success toast messages match UI-SPEC copywriting contract. EditRecipe error state uses correct CSS class and heading. All pages have working 300ms fade-in transition via tw-animate-css composition API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This phase modifies existing client-side rendering only — no new data flows, API endpoints, or auth logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | CSS class injection via user-controlled data | mitigate | All className strings are hardcoded; no user-controlled class names exist. React auto-escapes all `{error}` expressions — no `dangerouslySetInnerHTML` used. |
| T-04-02 | Denial of Service | CSS specificity conflicts from class replacements | mitigate | Tailwind v4 utility-first approach eliminates specificity conflicts. All class replacements are single-token swaps. |
</threat_model>

<verification>
### Automated Checks
```bash
# Verify all critical bugs fixed
cd frontend
grep -c 'to="/new"' src/components/Header.tsx  # must be 0
grep -c 'to="/new"' src/pages/Profile.tsx       # must be 0
grep -c 'apiFetch' src/pages/Profile.tsx         # must be 0
grep -c '@ts-nocheck' src/pages/Profile.tsx      # must be 0

# Verify toast messages match UI-SPEC
grep "Account created successfully" src/pages/Register.tsx
grep "Recipe published successfully" src/pages/CreateRecipe.tsx
grep "Recipe updated successfully" src/pages/EditRecipe.tsx

# Verify CSS typo fixed
grep -c "text-text-cancel" src/pages/EditRecipe.tsx  # must be 0

# Verify animate-fade-in fully replaced
grep -r "animate-fade-in" src/  # must return empty (grep returns error code 1 = no match)
grep -r "animate-in fade-in" src/  # must show 9+ results

# TypeScript check
npx tsc --noEmit 2>&1 | grep -v "RecipeForm.tsx"  # only pre-existing errors remain
```

### Responsive Design Verification (Manual Audit)
The RESEARCH.md audit confirms 0 layout breakages across all 8 pages + Header + Footer at mobile (375px), tablet (768px), and desktop (1280px). No responsive CSS changes are needed.
</verification>

<success_criteria>
1. `grep -r "apiFetch" frontend/src/` returns empty — no remaining apiFetch references in codebase
2. `grep -r 'to="/new"' frontend/src/components/ frontend/src/pages/` returns empty — no dead /new links
3. All toast messages match UI-SPEC §Copywriting Contract exact strings
4. `grep -r "animate-fade-in" frontend/src/` returns empty — no-op class fully replaced
5. `cd frontend && npx tsc --noEmit` — zero new TypeScript errors from modified files
6. All 10 modified files contain only the targeted changes; no refactoring of unrelated code
</success_criteria>

<output>
Create `.planning/phases/04-polish-ux/04-01-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

| Category | Artifact | Location |
|----------|----------|----------|
| Fixed route | `to="/create"` on NavLink | frontend/src/components/Header.tsx:42 |
| Fixed route | `to="/create"` on Link CTA | frontend/src/pages/Profile.tsx:202 |
| Fixed import | `import { api }` replacing apiFetch | frontend/src/pages/Profile.tsx:11 |
| Fixed API call | `api.get('/api/recipes').then(r => r.data)` | frontend/src/pages/Profile.tsx:31 |
| Fixed API call | `api.delete(...)` | frontend/src/pages/Profile.tsx:62 |
| Fixed toast | `'Account created successfully!'` | frontend/src/pages/Register.tsx:47 |
| Fixed toast | `'Recipe published successfully!'` | frontend/src/pages/CreateRecipe.tsx:21 |
| Fixed toast | `'Recipe updated successfully!'` | frontend/src/pages/EditRecipe.tsx:47 |
| Fixed CSS class | `text-text-custom` (was text-text-cancel) | frontend/src/pages/EditRecipe.tsx:105 |
| Fixed heading | `"Unable to Load Recipe"` | frontend/src/pages/EditRecipe.tsx:105 |
| Fixed class | `animate-in fade-in duration-300` on 9 page roots | 9 files in frontend/src/pages/ |
