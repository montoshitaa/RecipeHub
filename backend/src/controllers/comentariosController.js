// TODO: Implement getComentarios (comments for a recipe)
const getComentarios = (req, res) => {
  res.status(501).json({ message: 'Not implemented' });
};

// TODO: Implement createComentario (add comment with rating)
const createComentario = (req, res) => {
  res.status(501).json({ message: 'Not implemented' });
};

// TODO: Implement deleteComentario (owner-only delete)
const deleteComentario = (req, res) => {
  res.status(501).json({ message: 'Not implemented' });
};

module.exports = { getComentarios, createComentario, deleteComentario };
