// TODO: Implement getRecetas (list with filters/pagination)
const getRecetas = (req, res) => {
  res.status(501).json({ message: 'Not implemented' });
};

// TODO: Implement createReceta (create new recipe)
const createReceta = (req, res) => {
  res.status(501).json({ message: 'Not implemented' });
};

// TODO: Implement getRecetaById (single recipe with author)
const getRecetaById = (req, res) => {
  res.status(501).json({ message: 'Not implemented' });
};

// TODO: Implement updateReceta (owner-only update)
const updateReceta = (req, res) => {
  res.status(501).json({ message: 'Not implemented' });
};

// TODO: Implement deleteReceta (owner-only delete)
const deleteReceta = (req, res) => {
  res.status(501).json({ message: 'Not implemented' });
};

module.exports = { getRecetas, createReceta, getRecetaById, updateReceta, deleteReceta };
